{% include 'header' %}

import UIKit

class {{ module_info.name }}ViewController: UIViewController, {{module_info.name}}PresenterToView {

    var viewToPresenter: {{ module_info.name }}ViewToPresenter!

    // MARK: Life cycle
    override func viewDidLoad() {
        super.viewDidLoad()
        viewToPresenter.viewIsReady()
    }


    // MARK: {{module_info.name}}PresenterToView
    func setupInitialState() {
    }
}
