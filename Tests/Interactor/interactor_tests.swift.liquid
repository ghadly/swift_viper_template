{% include 'header' %}

{% include 'tests_import_header' %}

class {{ module_info.name }}InteractorTests: XCTestCase {


    var mockInteractorProtocol = {{ module_info.name }}MockPresenter()
    var interactor = {{ module_info.name }}Interactor()

    override func setUp() {
        super.setUp()
        interactor.interactorToPresenter = mockInteractorProtocol
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

    class {{ module_info.name }}MockPresenter: {{ module_info.name }}InteractorToPresenter {

    }
}
