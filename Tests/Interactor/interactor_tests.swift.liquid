{% include 'tests_import_header' %}

class {{ module_info.name }}InteractorTests: XCTestCase {
    // MARK: Properties
    var interactor = {{ module_info.name }}Interactor()
    var interactorOutputMock = InteractorOutputMock()

    // MARK: Lifecycle
    override func setUp() {
        super.setUp()
        interactor.interactorOutput = interactorOutputMock
        // Add additional setup logic if required
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

    // MARK: Tests
    // TODO: ADD YOUR TESTS HERE

    // MARK: Mocked Classes
    class InteractorOutputMock: {{ module_info.name }}InteractorOutput {
        
    }
}
