{% include 'tests_import_header' %}

class {{ module_info.name }}PresenterTest: XCTestCase {
    // MARK: Properties
    var presenter: {{ module_info.name }}Presenter!
    var viewInputMock = ViewInputMock()
    var interactorInputMock = InteractorInputMock()
    var routerInputMock = RouterInputMock()

    // MARK: Lifecycle
    override func setUp() {
        super.setUp()
        presenter = {{ module_info.name }}Presenter(with: interactorInputMock, routerInput: routerInputMock)
        presenter.viewInput = viewInputMock
        // Add additional setup logic if required
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

    // MARK: Tests
    // TODO: ADD YOUR TESTS HERE

    // MARK: Mocked Classes
    class ViewInputMock: {{ module_info.name }}ViewInput {
        func setupInitialState() {
            
        }
    }

    class InteractorInputMock: {{ module_info.name }}InteractorInput {

    }

    class RouterInputMock: {{ module_info.name }}RouterInput {

    }
}
